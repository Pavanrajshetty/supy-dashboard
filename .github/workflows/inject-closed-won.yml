name: Inject Closed Won Into Leads Master

on:
  push:
    branches:
      - main
    paths:
      - 'data/Hubspot/Closed-won/**'
      - 'scripts/inject_closed_won.py'
      - '.github/workflows/inject-closed-won.yml'
  workflow_dispatch:

jobs:
  inject-closed-won:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Closed Won Injector
        env:
          FULL_REBUILD: "false"
        run: python scripts/inject_closed_won.py

      - name: Commit processed files if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add src/data/processed/leads_master/

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "inject closed won into leads master"
            git pull --rebase origin main
            git push origin main
          fi
